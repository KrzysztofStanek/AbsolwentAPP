<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wsizoprojekt.absolwent">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".autoryzacja"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".rejestracjaActivity" />
        <activity android:name=".tablica" />
        <activity android:name=".oaplikacji" />
        <activity android:name=".oaplikacji_regulamin" />
        <activity android:name=".oaplikacji_politykaprywatnosci" />
        <activity android:name=".oaplikacji_autorzy" />
        <activity android:name=".edycja_profilu_activity" />
        <activity android:name=".profil" />
        <activity android:name=".imprezy_w_okolicy" />
        <activity android:name=".dodaj_impreze" />
        <activity android:name=".chetni"></activity>
    </application>

</manifest>